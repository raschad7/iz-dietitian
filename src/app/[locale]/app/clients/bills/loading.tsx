import { PageLoading } from '@/components/layout/page-loading';

/** The Bills register's wait — the same one the client register shows. */
export default function BillsLoading() {
  return <PageLoading />;
}
