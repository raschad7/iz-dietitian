import { PageLoading } from '@/components/layout/page-loading';

/** The wait for one client's record, opened from the register or a link. */
export default function ClientRecordLoading() {
  return <PageLoading />;
}
