import { PageLoading } from '@/components/layout/page-loading';

/** What fills the page while a client's appointments are read. */
export default function AppointmentsLoading() {
  return <PageLoading />;
}
